diff --git a/node_modules/@databricks/ai-sdk-provider/dist/index.js b/node_modules/@databricks/ai-sdk-provider/dist/index.js
index 807a2b4..266592d 100644
--- a/node_modules/@databricks/ai-sdk-provider/dist/index.js
+++ b/node_modules/@databricks/ai-sdk-provider/dist/index.js
@@ -1640,8 +1640,8 @@ const convertUserMessage = (message) => {
 			if (part.mediaType.startsWith("image/")) {
 				const url = toHttpUrlString(part.data);
 				if (url) content.push({
-					type: "image",
-					image_url: url
+					type: "image_url",
+					image_url: {url: url}
 				});
 			}
 			break;
@@ -1665,8 +1665,8 @@ const convertAssistantMessage = async (message) => {
 			if (part.mediaType.startsWith("image/")) {
 				const url = toHttpUrlString(part.data);
 				if (url) contentItems.push({
-					type: "image",
-					image_url: url
+					type: "image_url",
+					image_url: {url: url}
 				});
 			}
 			break;
@@ -1714,6 +1714,7 @@ const toHttpUrlString = (data) => {
 	if (data instanceof URL) return data.toString();
 	if (typeof data === "string") {
 		if (data.startsWith("http://") || data.startsWith("https://")) return data;
+		if (data.startsWith("data:")) return data;
 	}
 	return null;
 };
